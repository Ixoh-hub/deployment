# Paste these lines as requirements.txt in the GitHub repo Streamlit deploys (your deployment repo).
# 1) In Streamlit Cloud → redeploy → Advanced settings → set Python to 3.12 (or 3.11).
#    Building pandas from source on Python 3.14 is what caused your long hang + failure.
# 2) App settings → Main file must be streamlit_app.py (not app.py — that is FastAPI).
# 3) Remove seaborn==0.14.1 entirely unless you truly need it; it breaks uv on Cloud.

streamlit==1.44.1
pandas==2.2.3
numpy==2.2.4
scikit-learn==1.6.1
lightgbm==4.6.0
joblib==1.4.2
