# Streamlit Community Cloud: replace requirements.txt with ONLY these lines (no sklearn, no seaborn).
# Main file MUST be streamlit_app.py. Prefer Python 3.12 in Advanced settings (avoids pandas source builds on 3.14).

streamlit==1.44.1
pandas==2.2.3
numpy==2.2.4
lightgbm==4.6.0
joblib==1.4.2
